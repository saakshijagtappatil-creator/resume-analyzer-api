# =============================================================
# .env.example — COMMIT THIS FILE
# Copy to .env, fill in values, NEVER commit .env
# =============================================================

# -------------------------------------------------------------
# Spring Profile (dev | staging | prod)
# -------------------------------------------------------------
SPRING_PROFILE=dev

# -------------------------------------------------------------
# Database (PostgreSQL via Neon or Supabase)
# -------------------------------------------------------------
DB_URL=jdbc:postgresql://localhost:5432/resumeanalyzer_dev
DB_USERNAME=postgres
DB_PASSWORD=

# -------------------------------------------------------------
# Redis (self-hosted inside OKE cluster)
# -------------------------------------------------------------
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# -------------------------------------------------------------
# RabbitMQ (self-hosted inside OKE cluster)
# -------------------------------------------------------------
RABBITMQ_HOST=localhost
RABBITMQ_PORT=5672
RABBITMQ_USERNAME=guest
RABBITMQ_PASSWORD=guest
RABBITMQ_VHOST=/

# -------------------------------------------------------------
# JWT
# -------------------------------------------------------------
JWT_SECRET=
JWT_EXPIRATION_MS=86400000

# -------------------------------------------------------------
# AI (Anthropic Claude)
# -------------------------------------------------------------
AI_API_KEY=
AI_MODEL=claude-opus-4-6
AI_BASE_URL=https://api.anthropic.com
AI_MAX_TOKENS=4096
AI_TIMEOUT_SECONDS=60

# -------------------------------------------------------------
# File Upload
# -------------------------------------------------------------
FILE_UPLOAD_DIR=/tmp/resumes

# -------------------------------------------------------------
# Google OAuth2
# -------------------------------------------------------------
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here