# docker-compose.yml
# Place this file in the root of your project (next to pom.xml)

version: '3.8'

services:

  postgres:
    image: postgres:16-alpine
    container_name: resume-analyzer-postgres
    environment:
      POSTGRES_DB: resumeanalyzer_dev
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - resume-analyzer-network

  redis:
    image: redis:7-alpine
    container_name: resume-analyzer-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - resume-analyzer-network

  rabbitmq:
    image: rabbitmq:3.13-management-alpine
    container_name: resume-analyzer-rabbitmq
    environment:
      RABBITMQ_DEFAULT_USER: guest
      RABBITMQ_DEFAULT_PASS: guest
    ports:
      - "5672:5672"
      - "15672:15672"
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - resume-analyzer-network

volumes:
  postgres_data:
  redis_data:
  rabbitmq_data:

networks:
  resume-analyzer-network:
    driver: bridge